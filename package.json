{
  "name": "cleanshot",
  "private": true,
  "version": "0.1.0",
  "description": "CleanShot — AI-powered forklift image processing platform",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "packageManager": "pnpm@11.1.2",
  "scripts": {
    "dev:web": "pnpm --filter cleanshot-web dev",
    "build:web": "pnpm --filter cleanshot-web build",
    "typecheck": "pnpm --filter cleanshot-web exec tsc --noEmit",
    "test": "pnpm --filter cleanshot-web test --run",
    "lint": "pnpm --filter cleanshot-web lint",
    "audit": "pnpm audit --audit-level=high"
  }
}
