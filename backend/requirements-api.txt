# =============================================================================
#  API container deps
#  Usage (local Windows venv): pip install -r requirements-api.txt
# =============================================================================

-r requirements-base.txt

# --- Web framework ---
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1

# --- File upload handling (multipart/form-data parsing for direct uploads if needed) ---
python-multipart>=0.0.17,<0.1
openai>=1.50,<2 
anthropic>=0.40,<1 
