# =============================================================================
#  Worker container deps
#  Usage (local WSL2 venv): pip install -r requirements-worker.txt
#
#  Prerequisite on WSL2 (Ubuntu) for local dev:
#    sudo apt update && sudo apt install -y libvips-dev
# =============================================================================

-r requirements-base.txt

# --- Image processing ---
# pyvips: libvips bindings, releases the GIL for true parallelism
# Used for the Resize tab and any future post-processing
pyvips>=2.2,<3
# Pillow as a fallback for formats pyvips is awkward with
pillow>=11.0,<12

# --- Multi-provider scan SDKs ---
# Gemini is already in base via google-genai; these add the other two providers
openai>=1.50,<2
anthropic>=0.40,<1
