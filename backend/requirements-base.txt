# =============================================================================
#  Shared deps — both API and worker need these
# =============================================================================

# --- Config & validation ---
pydantic>=2.9,<3
pydantic-settings>=2.5,<3

# --- Logging ---
structlog>=24.4,<25

# --- Retry primitives ---
tenacity>=9.0,<10

# --- ID generation (UUIDv7 for sortable session/asset IDs) ---
uuid6>=2024.1.12

# --- Google Cloud SDKs ---
# google-genai 1.70.0 specifically; 1.67.0 has a typing-extensions bug under py3.12
google-genai==1.70.0
google-cloud-storage>=2.18,<3
google-auth>=2.35,<3

# --- Redis client (used by both API for /jobs reads and worker via Arq) ---
redis>=5.2,<6

# --- Job queue (API enqueues jobs, worker consumes them) ---
arq>=0.26,<1